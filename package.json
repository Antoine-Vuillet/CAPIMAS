{
  "dependencies": {
    "cypress": "^13.16.0",
    "mocha": "^10.8.2"
  },
  "devDependencies": {
    "jest": "^29.7.0"
  },
  
    "scripts": {
      "test": "jest --coverage",
      "coverage": "jest --json --coverage --outputFile=report.json"
    }
  
}
